from django.urls import path
from . import views

urlpatterns = [
    # path('create/'), #리뷰 생성
    # path('<int:review_id>/edit/'), #리뷰 수정
    # path('<int:review_id>/delete/'), #리뷰 삭제
    # path('all/'), #전체 리뷰 출력
    # path('<int:review_id>/'), #review_id에 해당하는 리뷰 출력
]